// Bundled Foundation logo assets. require() paths are relative to this file
// (lib/), which Metro resolves at bundle time.
export const logos = {
  /** Full lockup for light backgrounds (splash plate). */
  primary: require("../assets/logo/althaqalayn_logo_primary.png"),
  /** Gold emblem on transparent — reads on the green header/sidebar. */
  icon: require("../assets/logo/althaqalayn_icon.png"),
};
