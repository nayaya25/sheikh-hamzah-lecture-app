// Shared shell chrome dimensions — single source for the tab bar and the
// mini-player so their offsets never drift out of sync with each other.

export const TAB_BAR_HEIGHT = 64;
export const MINI_PLAYER_HEIGHT = 58;
export const MINI_PLAYER_GAP = 8;
